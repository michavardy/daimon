# Roadmap

## Evolutionary branch directives
it would be cool to have a special type of directive thats branched such that it provides an experimental set of changes that must pass a test, pref a non human in the loop coding condition.  if the test passes than the branch could be adopted as main, if the branch fails, it should roll back to the branch initial commit.  branches should be mergable and handle git conflicts?

## Multi-model experimentation
including local models, smaller models, and thinking models for different tasks.  some tasks are offline, where some tasks are time sensitive.